// BriefReportView.swift

import SwiftUI

struct BriefReportView: View {
    var body: some View {
        // TODO: 这里要用CoreData获取对应的数据
        CardSeperatorView(title: "本月花销", contentView: AnyView(Text("TODO 添加ExinThisMonthReportView")))
    }
}
