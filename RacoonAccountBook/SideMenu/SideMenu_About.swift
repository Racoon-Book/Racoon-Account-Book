// Acknowledgement.swift

import SwiftUI

struct SideMenu_About: View {
    var body: some View {
        NavigationView {
            VStack {
                Text("About")
            }
        }
        .navigationTitle("设置")
        .navigationBarTitleDisplayMode(.inline)
    }
}
